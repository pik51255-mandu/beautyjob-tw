# beautyjob.tw 콘텐츠 공장 — 진행 대장

최종 갱신: 2026-08-21

---

## Phase 0 — 준비 확인 결과

### 0-1 / 0-2 소스 파일

| 파일 | 크기 | 상태 |
|---|---:|---|
| `sources/01_research_additions.md` | 26,367 B | ✓ |
| `sources/02_taiwan_bible.md` | 21,353 B | ✓ |
| `sources/03_seeding_plan_30.md` | 8,662 B | ✓ |
| `sources/04_exam_mapping.md` | 7,522 B | ✓ |
| `sources/05_067003A13.pdf` | — | **✗ 확보 실패 (아래 참조)** |

### 0-3 題庫 PDF 확보 — **완료 (2026-08-21, 수동 확보)**

지시서의 두 경로를 모두 시도했고 둘 다 실패했다.

| 경로 | 시도 | 결과 |
|---|---|---|
| a) 가오슝시정부 게시 사본 | `curl -L` + 브라우저(Playwright) 2회 | **HTTP 404** — 해당 파일이 서버에서 내려감. 호스트(`orgws.kcg.gov.tw`, 223.200.91.232)는 살아 있으나 파일 경로가 없음 |
| b) 공식 허브 `techbank.wdasec.gov.tw` | `curl` + 브라우저 + DNS 우회 | **SNI 기반 네트워크 차단**. 공개 리졸버로는 `210.69.37.94` 로 정상 해석되고 TCP 443 도 연결되지만, TLS Client Hello 직후 연결이 끊긴다(`SSL_ERROR_SYSCALL`). 도메인 허용목록 추가 필요 |

**정정(2026-08-21):** 최초 보고에서 "도메인 미해석"으로 적었으나, 공개 리졸버(8.8.8.8/1.1.1.1)로는 정상
해석된다. 로컬 리졸버와 SNI 필터에서 막힌 것이며 **도메인은 실재한다**. 즉 b) 는 허용목록 추가로 해소 가능하다.

대체 경로 도달성 실측: `www.block.tw` **HTTP 200(도달 가능)**, `www.quizfun.co` **차단**.

**해소(2026-08-21 저녁): 선후님이 직접 받아 세션에 첨부.** 11월 대만 도착까지 미룰 필요가
없어졌다. `content-factory/sources/05_067003A13.pdf` (1,346,214 bytes, 29쪽) 로 배치했고,
텍스트 추출로 `女子美髮`·`V114112113` 존재를 확인했다. 1-a lint 규칙(500KB 이상)도 통과한다.

이 절의 차단 기록은 지우지 않고 남긴다 — 원격 확보가 왜 불가능했는지가 版次 갱신 때 다시 필요하다.

~~도착 후 조치(사람)~~ (해소됨): 브라우저에서 아래 중 하나로 `067003A13`(版次 V114112113) PDF를 받아
`content-factory/sources/05_067003A13.pdf` 로 저장.
- 技能檢定中心 測試參考資料 페이지에서 직종코드 `06700` 검색
- 또는 `skill.tcte.edu.tw` (전능검정 사이트, 현재 접속 가능) 경유

**차단 범위**: 라인 B(기출 해설) 전량과 Phase 0-4(단원·문항수 확정)가 이 파일에 의존한다.
라인 A(이론 30편)는 의존하지 않는다.

### 0-4 단원·문항수 확정 — **완료 (2026-08-21)**

공식 PDF 실물 파싱으로 확정. **工作項目 01~09 / 총 608題.**

| 코드 | 단원 | 문항수 |
|---|---|---|
| 01 | 人體生理 | 69 |
| 02 | 頭髮生理 | 89 |
| 03 | 美髮器材 | 50 |
| 04 | 頭髮洗護 | 88 |
| 05 | 識圖 | 10 |
| 06 | 剪髮 | 73 |
| 07 | 燙髮 | 97 |
| 08 | 染髮 | 80 |
| 09 | 整髮 | 52 |
| | **합계** | **608** |

**정정: 818題 → 608題.** 근거는 공식 PDF `067003A13` V114112113 실물 파싱이며,
`04_exam_mapping.md` v1 의 818題·工作項目 01~13 은 **구판 기준**이었다.
구판 12(化粧品的認識)·13(公共衛生)은 현행 파일에 없다 — 美容美髮 공동과목으로 분리된 것으로 보인다.
**해당 주제 해설은 별도 題庫 확보 전까지 착수 금지.**

원천 데이터: `sources/05_067003A13.json` / 파서: `scripts/parse_exam_bank.py` (재실행 가능).

### 0-5 글 게재 방식 — **A안 채택 확정** (2026-08-21, 구현은 여전히 승인 전 금지)

리포 현황: React SPA(wouter) + Express + Drizzle(MySQL) 구성이며,
이미 `server/salonPages.ts` 로 **서버 렌더 HTML 페이지**(`/salons`·`/area/*`·`/salon/*`·`/stats`)를
SPA 정적 서빙보다 먼저 등록해 내보내는 패턴이 자리잡혀 있다. 이 패턴이 SEO 검증(수용 테스트)까지
갖춰져 있어 재사용 가치가 가장 크다.

| 안 | 구조 | 장점 | 단점 | 적합도 |
|---|---|---|---|---|
| **A. DB `articles` 테이블 + 서버 렌더** | 마이그레이션으로 `articles` 신설(slug·level·series·keywords·body·publishedAt), `salonPages.ts` 와 같은 방식으로 `/articles/:slug` 서버 렌더 | 기존 SSR·JSON-LD·수용 테스트 자산 그대로 재사용. 검색·필터·레벨 태그가 SQL로 자연스러움. 발행/비공개 상태 관리 용이 | 마이그레이션 필요. 원고 수정 시 DB 적재 절차 필요 | **권장** |
| B. 정적 마크다운 렌더링 | `content-factory/drafts/**.md` 를 빌드 시 HTML 로 변환해 정적 서빙 | DB 무변경. 원고=단일 진실원천, git 이력이 곧 개정 이력 | 목록·필터·검색을 직접 구현해야 함. 글 수가 늘면 빌드 산출물 관리 부담 | 차선 |
| C. 기존 `community_posts` 확장 | `category` enum 에 값 추가하고 기존 커뮤니티 모델에 편입 | 신규 테이블 없음. 댓글·조회수 기능 즉시 재사용 | `authorId` 필수 등 회원 글 전제와 충돌. 레벨·시리즈·slug 개념이 없어 스키마 오염. SEO 구조(서버 렌더·JSON-LD)를 새로 붙여야 함 | 비권장 |

**A안 채택 확정.** `articles` 테이블 + `/articles/:slug` 서버 렌더. 설계만 진행하고 구현·DB 변경·배포는 금지.

---

## slug 확정 목록 (R6 — 최초 1회 고정, 이후 변경 금지)

내부링크는 `/articles/{slug}` 형식을 쓴다.

### 라인 A — 이론 30편

| # | 제목 | slug | level | 상태 |
|---:|---|---|---|---|
| 1 | 頭髮結構全解析：毛鱗片、皮質層、髓質層 | `hair-structure-basics` | 初級 | **검증통과** |
| 2 | 頭髮的四種鍵結：氫鍵、鹽鍵、雙硫鍵、胜肽鍵 | `hair-bonds-four-types` | 初級 | **검증통과** |
| 3 | pH與美髮：為什麼護髮都是弱酸性？ | `ph-and-hair-care` | 初級 | **검증통과** |
| 4 | 一劑二劑在做什麼：氧化與還原入門 | `oxidation-reduction-basics` | 初級 | **검증통과** |
| 5 | 洗髮精與潤髮乳的化學：界面活性劑入門 | `surfactant-basics` | 初級 | **검증통과** |
| 6 | 毛髮生長週期：生長期、退化期、休止期 | `hair-growth-cycle` | 初級 | **검증통과** |
| 7 | 台灣人的頭皮現實：油性頭皮判斷與基礎養護 | `oily-scalp-taiwan` | 初級 | **검증통과** |
| 8 | 髮質診斷基本功：看、摸、拉三步驟 | `hair-diagnosis-basics` | 初級 | **검증통과** |
| 9 | 多孔性是什麼：為什麼受損髮上色特別快 | `hair-porosity` | 初級 | **검증통과** |
| 10 | 染髮前48小時：皮膚測試完整指南 | `patch-test-guide` | 初級 | **검증통과** |
| 11 | 燙髮三步驟：還原→重組→氧化的完整原理 | `perm-three-steps` | 中級 | **검증통과** |
| 12 | 軟化判斷完全指南：測試軟化的正確方法 | `softening-test-guide` | 中級 | **검증통과** |
| 13 | 還原劑比較：TGA、半胱胺酸、半胱胺鹽酸鹽 | `reducing-agents-compared` | 中級 | **검증통과** |
| 14 | 冷燙、溫塑燙、熱塑燙：原理層面的完整差別 | `perm-types-compared` | 中級 | **검증통과** |
| 15 | 離子燙與縮毛矯正：藥水、溫度、設計的差異 | `straightening-compared` | 中級 | **검증통과** |
| 16 | 染髮色彩學：色相環與補色中和 | `color-theory-basics` | 中級 | **검증통과** |
| 17 | 底色理論：紅→橘→黃的祕密 | `undertone-theory` | 中級 | **검증통과** |
| 18 | 雙氧乳設計邏輯：3%、6%、9%、12%怎麼選 | `developer-volume-guide` | 中級 | **검증통과** |
| 19 | 漂髮科學：漂粉+雙氧的化學與過硫酸鹽 | `bleaching-science` | 中級 | **검증통과** |
| 20 | 蓋白髮的科學：白髮為什麼特別難染 | `grey-coverage-science` | 中級 | **검증통과** |
| 21 | 矯色與紫色洗髮精：原理與極限 | `toning-purple-shampoo` | 中級 | **검증통과** |
| 22 | 熱損傷科學：幾度開始不可逆？ | `heat-damage-science` | 中級 | **검증통과** |
| 23 | 結構式護髮的化學：Olaplex、K18差在哪 | `bond-builder-chemistry` | 高級 | **검증통과** |
| 24 | 卡色與漂髮發熱的隱形兇手：金屬離子與硬水 | `metallic-salts-hard-water` | 高級 | **검증통과** |
| 25 | 酸熱離子護的真相：乙二醛酸原理與安全爭議 | `glyoxylic-acid-truth` | 高級 | **검증통과** |
| 26 | 設計師的職業安全：PPD、過硫酸鹽與你的手和肺 | `stylist-occupational-safety` | 高級 | **검증통과** |
| 27 | 高溫潮濕與髮型：濕氣、氫鍵、抗毛躁的科學 | `humidity-frizz-science` | 高級 | **검증통과** |
| 28 | 為什麼台灣夏天褪色特別快：UV、汗水與護色設計 | `uv-color-fading` | 高級 | **검증통과** |
| 29 | 受損髮三層次診斷：表面、內部、鍵結的護理設計 | `damaged-hair-three-layers` | 高級 | **검증통과** |
| 30 | 設計師不能說的話：化粧品廣告法規完全指南 | `cosmetic-ad-regulations` | 高級 | **검증통과** |

### 라인 B — 기출 해설 (07 燙髮 확정분)

單元 구성이 확정돼 slug 를 고정한다. 형식은 `exam-{단원}-{연번}`.

| 편 | 제목 | slug | 문항수 | 상태 |
|---|---|---|---|---|
| 燙髮① | 藥劑作用·軟化·試捲 | `exam-perm-01` | 30 | **작성 완료 (P2)** |
| 燙髮② | 捲法·分區·底盤·角度 | `exam-perm-02` | 27 | 대기 (Phase 3 승인 후) |
| 燙髮③ | 髮質診斷·燙前處理 | `exam-perm-03` | 21 | 대기 |
| 燙髮④ | 燙後處理·安全·藥劑管理 | `exam-perm-04` | 19 | 대기 |

한국어 병기판은 `-ko` 접미(`exam-perm-01-ko`)로 `drafts/exam-ko/` 에 둔다.
나머지 8개 단원의 slug 는 각 편 착수 시 이 표에 추가 고정한다.

---

## 진행 대장

| 편 | 제목 | slug | 상태 | 字數 | lint | 날짜 |
|---|---|---|---|---:|---|---|
| A-1 | 頭髮結構全解析 | `hair-structure-basics` | 검증통과 | 1,889 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-2 | hair-bonds-four-types | `hair-bonds-four-types` | 검증통과 | 2,641 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-3 | ph-and-hair-care | `ph-and-hair-care` | 검증통과 | 3,157 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-4 | oxidation-reduction-basics | `oxidation-reduction-basics` | 검증통과 | 2,742 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-5 | surfactant-basics | `surfactant-basics` | 검증통과 | 3,207 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-6 | hair-growth-cycle | `hair-growth-cycle` | 검증통과 | 2,199 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-7 | oily-scalp-taiwan | `oily-scalp-taiwan` | 검증통과 | 2,643 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-8 | hair-diagnosis-basics | `hair-diagnosis-basics` | 검증통과 | 3,519 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-9 | hair-porosity | `hair-porosity` | 검증통과 | 2,863 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-10 | patch-test-guide | `patch-test-guide` | 검증통과 | 3,467 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-11 | perm-three-steps | `perm-three-steps` | 검증통과 | 4,456 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-12 | softening-test-guide | `softening-test-guide` | 검증통과 | 4,115 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-13 | reducing-agents-compared | `reducing-agents-compared` | 검증통과 | 3,794 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-14 | perm-types-compared | `perm-types-compared` | 검증통과 | 4,480 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-15 | straightening-compared | `straightening-compared` | 검증통과 | 4,465 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-16 | color-theory-basics | `color-theory-basics` | 검증통과 | 3,444 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-17 | undertone-theory | `undertone-theory` | 검증통과 | 3,994 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-18 | developer-volume-guide | `developer-volume-guide` | 검증통과 | 3,543 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-19 | bleaching-science | `bleaching-science` | 검증통과 | 3,948 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-20 | grey-coverage-science | `grey-coverage-science` | 검증통과 | 5,062 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-21 | toning-purple-shampoo | `toning-purple-shampoo` | 검증통과 | 4,031 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-22 | heat-damage-science | `heat-damage-science` | 검증통과 | 4,005 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-23 | bond-builder-chemistry | `bond-builder-chemistry` | 검증통과 | 5,564 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-24 | metallic-salts-hard-water | `metallic-salts-hard-water` | 검증통과 | 5,431 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-25 | glyoxylic-acid-truth | `glyoxylic-acid-truth` | 검증통과 | 4,948 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-26 | stylist-occupational-safety | `stylist-occupational-safety` | 검증통과 | 5,461 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-27 | humidity-frizz-science | `humidity-frizz-science` | 검증통과 | 4,467 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-28 | uv-color-fading | `uv-color-fading` | 검증통과 | 5,050 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-29 | damaged-hair-three-layers | `damaged-hair-three-layers` | 검증통과 | 5,629 | 간체 0 / 금지어 0 | 2026-08-21 |
| A-30 | cosmetic-ad-regulations | `cosmetic-ad-regulations` | 검증통과 | 4,238 | 간체 0 / 금지어 0 | 2026-08-21 |
| B-1 | 燙髮篇 제1편(軟化·試捲) | — | **차단** (PDF 미확보) | — | — | — |

---

## 알려진 이슈 (해소됨)

- **lint.mjs 연산자 우선순위 버그 (2026-08-21 수정)**: `new Set("A" + "B".split("").filter(...))`
  에서 `.split()` 이 뒤 리터럴에만 걸려 배열이 문자열화되고, join 구분자인 ASCII 콤마가
  간체자 집합에 섞였다. 그 결과 본문에 반각 콤마만 있어도 "간체자 1종" 오탐이 났다.
  배치 ① 집필 에이전트 4명이 독립적으로 이 증상을 보고했고, 일부는 본문에서 콤마·영문
  화학명을 빼는 방식으로 회피했다. 두 리터럴을 괄호로 묶어 수정했고, 회피로 빠졌던
  `Toluene-2,5-diamine` 표기를 #10 에 복원했다.

## 배치 ② 에서 드러난 lint 이슈

1. **2-d %↔vol 규칙 오탐 (수정 완료)**: 서술문의 「把「6%」聽成「6vol」」처럼 *오류를 경고하는*
   문장까지 불일치로 잡았다. 대응을 단정하는 **표 행에서만** 판정하도록 좁혔고,
   표 안 오류(9%↔20vol)는 여전히 검출됨을 음성 대조로 확인했다.

2. **금지어 부분문자열 과검출 (미해결 — 판단 필요)**: `生髮` 를 부분문자열로 매칭하다 보니
   `新生髮根`·`原生髮`(둘 다 대만 현장 표준어: 뿌리 신생부 / 시술 이력 없는 모발)까지 걸린다.
   집필 담당 4명이 `新長出來的部分`·`新生區`·`還沒被染燙過的頭髮` 등으로 우회했다.
   결과 문장 자체는 자연스러우나 표준 업계어를 못 쓰는 상태다.
   같은 이유로 `刺激`(단독)·`醫療`(단독)도 회피 대상이 됐다 — 원래 금지 대상은
   `刺激毛囊`·`醫療級` 뿐이다. **R2 를 완화할지(예외 목록 도입) 여부는 선후님 판단 사항**이라
   임의로 바꾸지 않았다.

## 라인 A 이론 30편 — 전량 완료 (2026-08-21)

전 30편 lint 통과. 간체자 0 / 규제 금지어 0 / 教科書沒說的 전편 빈칸 유지 /
내부링크 전부 확정 slug 목록 내 / 雙氧乳 %↔vol 전 구간 일치.

- 초급 10편 · 중급 12편 · 고급 8편
- 총 中文 字數 약 122,000
- #30 은 R9 예외 적용: 금지어 14종이 전부 「」 인용 안에만 등장(인용 91개).
  인용 밖 `生髮` 2건은 화이트리스트 어휘(原生髮·新生髮)로 마스킹 후 잔존 0.

## v12 이력 소급 기록 (2026-08-21) — **정정판**

**최초 기록은 틀렸다.** 2026-08-21 저녁에 "1-a~1-d 대상이 전부 부재"라고 적었으나,
그건 **로컬 작업트리만 보고 `origin/main` 을 fetch 하지 않아서** 나온 결론이었다.
원격에는 다른 세션이 PR #4~#7 로 올린 작업이 있었고, 지시서 v12 의 전제는 **네 항목 모두 옳았다.**

| 항목 | 최초 기록(오류) | 실제 (origin/main 확인 후) |
|---|---|---|
| 1-a `sources/05_067003A13.pdf` | "처음부터 부재" | **실재했다.** 머지베이스 `c0cff77` 에 0바이트(empty blob `e69de29`)로 있었고, `72e9cf0` 에서 추가·`82587e1`(이번 세션 Phase 0)에서 삭제됐다. v12 시점에 작업트리에 없던 건 **내가 앞서 지웠기 때문**이지 존재한 적 없어서가 아니다 |
| 1-b 섹션명 혼재 | "혼재 없음" | **혼재 있음.** `ch01/zh/article-zh.md` 는 `## 課本沒說的事`, 라인 A 30편은 `## 教科書沒說的` |
| 1-c 서술·인용 유입 | "유입 없음" | **유입 있음.** ch01·ch02 의 해당 절은 AI 가 쓴 서술 + 논문 인용(Leerunyakul 2020 등)이 질문에 섞여 있다. 라인 A 30편만 깨끗했다 |
| 1-d `ch01`·`ch02` | "존재한 적 없음" | **존재.** `ch01/{ko,zh}/` · `review-ko/ch02/` — 본문 md, 일러 SVG·PNG 3종, 루프 mp4/webm, 카드 2장, 프리뷰 HTML |
| 1-d zh 렌더 이력 | "실행한 적 없음" | **실행됨.** `ch01/zh/article-zh.md` + `chapter-01-preview-zh.html` + `artifacts/ch01-preview-zh.artifact.html`. ch02 는 ko 만, zh 미렌더 |

**왜 놓쳤나**: 로컬 `git status` 는 clean 이었고 세션 내내 `git fetch` 를 하지 않았다.
두 작업 흐름(로컬=라인 A 이론 30편 / 원격=ch01·ch02 비주얼·렌더)이 서로를 모른 채 같은
`content-factory/` 를 채우고 있었다. 원격 기록에도 흔적이 있다 — v7 실행 기록의
"04 문서도 리포에 없음", "02 바이블 문서가 리포에 없어"는 반대편에서 이쪽을 못 본 것이다.

**재발 방지**: 콘텐츠 공장 작업 착수 전 `git fetch origin` 후 `origin/main` 과 대조한다.
"파일이 없다"는 판정은 로컬 트리만으로 내리지 않는다.

### v12 1-a~1-d 실제 조치 (정정 후)

- **1-a**: 이미 `82587e1` 에서 삭제돼 있었고, 머지도 그 삭제를 유지한다(원격 쪽은 파일을 그대로
  들고 있었으나 "우리 쪽 삭제"가 이긴다). 결과 상태는 지시서가 원한 그대로다.
  규칙이 실제로 작동하는지는 0바이트 probe 파일을 만들어 양성 대조로 확인했다 —
  `✗ sources PDF 규칙 위반(1-a): __probe.pdf (0B — 500KB 미만)` 출력 후 probe 제거.
- **1-b**: `ch01/zh` 의 `課本沒說的事` → `教科書沒說的` 로 통일. md 1개 + 프리뷰 HTML 3개 +
  artifact HTML 3개까지 같이 고쳤다(파생물만 옛 표기로 남는 사고 방지). 잔존 0.
- **1-c**: ch01(ko·zh)·ch02(ko) 의 해당 절에서 AI 서술·인용을 걷어내고 유도 질문만 남긴 뒤
  `### 現場答（선후 작성）` / `> [미작성]` 슬롯을 넣었다. 라인 A 30편은 이미 적용돼 있었다.
  파생 HTML 6종도 같은 내용으로 패치했고, Chromium 으로 4종을 열어 제목 순서·슬롯 표시·JS 에러 0 을 확인했다.

  **걷어낸 것**: ch01 의 동아시아 모발 저항성 연구 인용(Leerunyakul & Suchonwanit, *Clin Cosmet
  Investig Dermatol* 2020)과 온도별 변성 수치, ch02 의 컬 이완 시간상수 인용(Wortmann 외,
  *Biophysical Journal* 2021)·환원율 수치. 사실 자체는 버릴 내용이 아니지만 **이 절에 있으면 안 되는
  형식**이라 뺐다. 원문은 머지 이전 커밋(`51fa90e` 계열)에 그대로 남아 있으니, 본문 서술부로 옮길지는
  ch01·ch02 를 만든 쪽에서 판단하면 된다. 임의로 옮기지 않았다.
  ch01/zh 본문 R1/R2 재검사: 간체 0 · 대륙어휘 0 · 금지어 0.
- **1-d**: 이 절이 그 기록이다.

## 사이트 도구 — 染髮調配計算機 (v12 2, 2026-08-21)

이론 글의 계산 파트를 사이트 도구로 옮긴 5번째 계산기. **콘텐츠 공장 산출물이 아니라
사이트 코드**이므로 여기에는 연결 관계만 남긴다.

- 경로 `/tools/color-mix` · 設計師工具(공개, OwnerGate 없음)
- 안전 레일이 참조하는 이론 글: `undertone-theory`(#17) · `bleaching-science`(#19) ·
  `grey-coverage-science`(#20). 세 slug 은 확정 목록 내.
- 다만 **링크는 아직 렌더하지 않는다** — `/articles/*` 라우트가 없어 404 가 되기 때문.
  `ColorMix.tsx` 의 `ARTICLES_LIVE` 상수만 true 로 바꾸면 붙는다. 게재 구현 시 함께 켤 것.
- UI 중국어 문구도 R1/R2 를 탄다: `node content-factory/lint-ui.mjs` (lint.mjs 의 용어
  목록을 그대로 import 한다 — 기준이 두 벌로 갈라지지 않게).

## v7 실행 기록 (2026-08-21)

### 라인 A — Manus 題庫 PDF 태스크

| 항목 | 결과 |
|---|---|
| A-1 실행 전 잔여 크레딧 | **조회 불가** — `usage.list`는 차감 내역(ledger)만 제공, 잔액 엔드포인트 없음. 대신 task_id별 실비로 소모량 산출 |
| A-2 태스크 생성 | `task_id: 55XABqrsKUDDu2P9SAmrXh` (지시 프롬프트 원문 그대로, 단발) |
| 태스크 결과 | **차단됨** — Manus가 techbank.wdasec.gov.tw 접속 차단 보고. 구조화 출력 `{blocked: true, notes: "차단됨"}` |
| A-3 PDF 저장·검증 | 미실행 (PDF 미확보) |
| A-4 소모 크레딧 | `usage.list`에 해당 task_id의 `type:"cost"` 항목 생성 확인, **credits 수치는 정산 대기** (조회 시점까지 미표시). 실행 시간 ~1.5분(생성 09:08 → stopped 09:09)으로 소액 추정. 다음 세션에서 재조회할 것 |
| A-5 단원·문항수 확정 | 미실행 (검증 불통과). 04 문서도 리포에 없음 — 차이 표 생성 불가 |
| A-6 준수 | 재시도·우회 지시 없이 종료 ✓ |

**후속 판단 필요:** 대만 정부 기능검정 사이트가 Manus 클라우드 IP를 차단하는 것으로 보임.
대안(수동 다운로드 후 파일 업로드, 대만 IP 경유 등)은 별도 승인 전 착수 금지 원칙에 따라 보류.

### 라인 B — #2 (4대 결합) 비주얼 파일럿 (전부 로컬, 크레딧 0)

| 단계 | 산출물 | 상태 |
|---|---|---|
| B-0 | `visual/style.json` — 사이트 테마(oklch)→hex 고정. 주 4색: 玫瑰金 `#b92846` / 深炭 `#0f1216` / 米白 `#fbf8f5` / 水藍 `#00a9b3` + 파생 틴트. 폰트 Noto Sans TC | ✓ |
| B-1 | `visual/char-{hydrogen,salt,disulfide,peptide}.svg` + `visual/characters.svg` (2×2 시트) — 기하 도형 기반, 생성 코드 `pilot-02/src/gen_svgs.py`로 완전 재현 가능 | ✓ |
| B-2 | `pilot-02/illust-01-strength.svg` (강도 비교) / `illust-02-hydrogen-water.svg` (물 만난 氫鍵 3패널) / `illust-03-perm-3steps.svg` (燙髮 3단계) | ✓ |
| B-3 | `pilot-02/loop-hydrogen-bond.mp4` — 1080×1350, H.264(yuv420p), 15fps, 8.0초, **179KB** (≤8MB ✓) | ✓ |
| B-4 | `pilot-02/card-01-concept.png` (훅: 吹的捲，為什麼隔天就沒了？) / `card-02-quiz-template.png` (4지선다 재사용 템플릿) — 1080×1350 | ✓ |
| B-5 | `pilot-02/previews/*.png` (SVG 8종 전부 1080px 미리보기) | ✓ |

**검증 결과**
- 루프 검증: 첫 프레임(t=0) vs 끝 프레임(t=8.0) 픽셀 diff **bbox None / max 0** — 완전 심리스 ✓
- 간체 lint: 총 한자 166종 전수 검사(OpenCC s2t 비교), 간체 **0** ✓ (초안의 元凶→元兇 교정 1건)
- 용어 lint: 氫鍵·鹽鍵·雙硫鍵·胜肽鍵·第一劑·第二劑·還原劑·氧化劑·捲·題庫·丙級 전부 존재, 대륙식 용어(氢键/烫 등) 0 ✓
- 참고: 02 바이블 문서가 리포에 없어 용어 대조는 v7 지시문의 용어 목록 기준으로 수행

**재현 파이프라인 (전부 로컬)**
`pilot-02/src/gen_svgs.py` (SVG 생성) → `src/render.mjs` (Chromium 렌더: 미리보기·카드·프레임, 요구 env: PW_DIR=playwright-core 설치 경로, FONT_CSS=Noto Sans TC @font-face) → ffmpeg(imageio-ffmpeg) 인코딩

### 미착수 (승인 대기)
- 배치 ② 진입 금지 유지
- 사이트 코드 변경·배포 없음
- Manus 추가 태스크 없음 (총 1건 생성)

---

## 라인 B 착수 — 題庫 파싱 + 燙髮篇① (2026-08-21)

### 데이터층

| 항목 | 결과 |
|---|---|
| PDF | `sources/05_067003A13.pdf` 1,346,214 bytes / 29쪽 (표지 1 + 본문 28) |
| 파서 | `scripts/parse_exam_bank.py` (pdfplumber 0.11.10, venv `~/.venvs/beautyjob-exam`) |
| JSON | `sources/05_067003A13.json` 196,538 bytes / 608문항 |
| 게이트 | G1·G2·G3·G4 **전부 PASS** — 실패 시 JSON 을 쓰지 않고 중단하도록 설계 |
| 라운드트립 | 608문항 전수 재조립 → 원문 대조 **불일치 0** (정보 손실·창작 0 증명) |

**첨자 분리(c항) 해법**: 라인 재조립이나 문자열 치환이 아니라 `extract_text(y_tolerance=9)` 한 줄로
해결했다. 아래첨자는 본문(size 12.0)보다 작은 size 6.96 이라 기본 y_tolerance(3)에서는 다음 줄로
떨어지지만, 9 로 올리면 같은 줄로 합쳐지며 x 순서대로 `H2O2` `HO4` `CO2` 가 자동 복원된다.
**PDF 전체에서 size<10 문자는 7자뿐이고 전부 23쪽의 08-034·08-037 에 속한다** — 지시서가 지목한
2문항 외 유사 패턴은 없다(전수 확인).

**tail**: 84건 보존. 전부 `。` 로 끝나는 문장 잔여부이며 stem 에 합치지 않았다.
보기 선행형(stem 없음)은 6건.

### 해설층 (P2)

- `drafts/exam/exam-perm-01.md` (번체) / `drafts/exam-ko/exam-perm-01-ko.md` (한국어 병기)
- 생성기 `scripts/build_exam_article.py` — **문항 텍스트는 JSON 에서만 가져온다.**
  사람이 쓰는 건 해설뿐이라, 재타이핑·기억 재구성이 구조적으로 불가능하다.
  검증: 기사 인용 30건 전부 JSON 원문과 문자 단위 일치, 정답 표기 30/30 일치.

### lint 조정 (라인 B 대응)

- **R2 범위 축소**: `kind === "exam"` 이면 인용부(`>` 로 시작하는 줄)를 R2 검사에서 뺀다.
  題庫 원문에 금지어가 실재하기 때문 — 전수 확인 결과 `治療` 3건(04-009·08-064·08-074),
  `再生` 2건(02-049·02-055). `生髮` 3건은 전부 `新生髮` 이라 기존 화이트리스트로 이미 통과.
  정부 공개 자료를 우리 규제 기준으로 고칠 수는 없으므로 원문 무결성을 우선한다.
- **R1 은 전체 적용**: 원문이 번체라 간체가 나오면 그건 우리 오타다.
- 양성 대조로 확인: 인용부에 `治療` 를 넣으면 통과, 같은 단어를 해설부에 넣으면 `✗ 금지어 治療`.
- exam 전용 게이트: B3 출처 문구 필수, B2 문항 30±10, 내부링크 2개 이상.

## 미결·후속

1. ~~05 題庫 PDF 확보~~ — **해소(2026-08-21).** 선후님이 직접 받아 첨부. 0-3·0-4 함께 종료.
2. **라인 B 잔여 21편** — 燙髮②③④ 및 나머지 8개 단원. **Phase 3 는 검수 후 별도 승인.**
   공동과목(化粧品·公共衛生) 계열은 題庫 미확보라 착수 금지.
3. 게재 방식 승인 (A/B/C 중) — 승인 전 구현 착수 금지.
4. **現場答 30편 작성** — 선후님 현장 답이 들어가야 `status=publishable` 이 된다. 현재 전편 draft.
5. 게재 구현 시 `ColorMix.tsx` 의 `ARTICLES_LIVE` 를 true 로 전환 (계산기 → 이론 글 링크).

---

## v14 실행 기록 (2026-08-21) — append-only

> 이 절부터 `progress.md` 는 **append-only** 다. 기존 줄을 다시 쓰지 않고, 정정도 새 줄로 붙인다.
> 근거: 리포 루트 `CLAUDE.md` 「세션 소유권 규칙」 4항.

### 1. #13 TGA 법규 절 핫픽스

〈特定用途化粧品成分名稱及使用限制表〉는 113.05.30 폐지(衛授食字第1131602017號),
113.07.01 시행으로 燙髮劑 성분이 〈化粧品成分使用限制表〉로 이관됐다. 폐지된 표를
현행처럼 인용하고 있었다 — 2026-08-21 낮에 내가 넣은 것이다.

- 표를 5행으로 교체(TGA 一般 8%·專業 11%·沖洗 2% / 酯類 8%·11%), **一般 급의 pH 7.0～9.5 누락 수정**
- 폐지 표 이름은 각주 1줄로만 잔존. 리포 전체 grep 결과 각주 1건 외 0건
- **정정 범위 확대**: 같은 오인용이 `drafts/exam/exam-perm-01.md`·`-ko`·`colorMixCalc.ts` 주석에도
  있었다. 라인 B 는 v14 범위 밖이지만 동일한 법규 오인용이라 **사실 정정만** 적용했다.
- **라이브 확인 결과: #13 은 라이브에 존재하지 않는다.** `/articles/*` 라우트가 없고
  `ARTICLES_LIVE=false` 라 `/articles/reducing-agents-compared` 는 404 를 렌더한다.
  즉 이 건은 라이브 오류가 아니라 리포 콘텐츠 오류였다. 배포는 했으나 화면 변화는 없다.

### 2. 雙氧乳 기준 단위 % 전환

- 단일 진실원천을 `percent` 로 옮겼다. `LiftVerdict` 에서 `vol` 필드 제거,
  `resolveVol`·`railForVol`·`SELECTABLE_VOLS`·`volToPercent` 폐기 →
  `resolvePercent`·`railForPercent`·`SELECTABLE_PERCENTS`·`percentToVol`
- vol 은 `DEVELOPERS` 환산표를 통해서만 만들어지는 표시용 파생값. 로직 진입점 0
- UI 선택줄: `依建議 / 3% / 6% / 9% / 12%`, vol 은 작은 보조 라벨로 병기
- **콘텐츠 계층은 이미 % 기준이었다.** 전수 조사(118건/18파일) 결과 vol 이 나오는 65줄 중
  64줄이 같은 줄에 % 를 병기하고 있고, `%（vol）` 순서 20줄 / `vol（%）` 순서 0줄.
  vol 단독은 #18 의 "vol 이란 무엇인가" 정의 1줄뿐이라 손대지 않았다.
  전수 목록: `content-factory/vol-notation-survey.md`

### 3. 도달 불가 분기 — 0건

환산표(3·6·9·12) ↔ 진단(3·6·9) ↔ 레일(12) 3자 대조를 테스트로 고정했다.
12% 는 진단이 추천하지 않지만 사용자 직접 선택으로 도달 가능하므로 죽은 분기가 아니다.

### 4. 법규 인용 대장

`content-factory/legal_citations.md` 신설. 현행 3 / 폐지 1 / 미확인 12.
lint 에 4-3 추가 — 본문에 법규명이 있는데 대장에 (파일, 법규명) 행이 없으면 실패.
실제로 #23·#25·#30 세 건을 잡아냈다(등재 후 통과).
**過氧化氫 12% 가 법정 상한인지는 미확인**으로 등재했고, 확인 전까지 코드·UI·본문 어디에도
「법정 상한」이라고 쓰지 않는다.

### 6. ch01·ch02 인용 이동

폐기하지 않고 본문 서술부로 옮겼다.

| 인용 | 이동 전 | 이동 후 |
|---|---|---|
| Leerunyakul & Suchonwanit 2020 (동아시아 모발 저항성) | ch01 ko·zh 教科書沒說的 | ch01 ko·zh **皮質層/코르텍스 절** |
| 온도별 변성 수치 130/150/200/230℃ | ch01 ko·zh 教科書沒說的 | ch01 ko·zh **毛鱗片/큐티클 절**(18-MEA 문단 뒤) |
| 이황화결합 환원 20~40% / 재산화 70~80% | ch02 ko 教科書沒說的 | ch02 ko **이황화결합 절** |
| Wortmann 2021 (컬 이완 시간상수) | ch02 ko 教科書沒說的 | ch02 ko **시술로 번역하면 절** |

파생 HTML 6종(프리뷰 3 + artifact 3)도 같이 갱신하고 Chromium 으로 렌더 확인.
검증: 세 글의 教科書沒說的 절 안 인용 0건 / 수치 0건.

---

## v15 실행 기록 (2026-08-22)

### 0. 접속 차단 — **DNS 원인 확정(코드 무죄)**

진단 3종 실측:

| 항목 | 결과 |
|---|---|
| `dig beautyjob.tw A` | `15.197.225.128` / `3.33.251.168` (레지스트라 포워딩, NS=`domaincontrol.com`=GoDaddy) |
| `dig www.beautyjob.tw CNAME` | `cname.manus.space.` ← **여기가 원인** |
| `curl -L https://beautyjob.tw` | apex 301 → `www` 302 → `manus.im/app-auth` (server: cloudflare = Manus 호스팅) |
| `curl -L https://beautyjob-tw.onrender.com` | **200, 리다이렉트 0** |

Render 원본은 멀쩡하다. 도메인이 아직 **Manus 호스팅(manus.space)을 가리키고 있어서**
Manus 플랫폼이 자기 앱 인증으로 튕기는 것이다. `/manus-oauth/callback` 은
**우리 리포에 없고 git 이력 전체에도 없다**(`git log --all -S`). 우리 코드는 `/api/oauth/callback` 을 쓴다.
→ 코드 제거 대상 없음. DNS 전환 필요(선후 작업, 별도 목록으로 전달).

검증: V1 홈 200/리다이렉트 0, V2 `/salons`·`/supply-map` 200/0, V3 robots.txt 크롤러 전면 허용
(단 **페이지 meta 가 아직 noindex** — 도메인 접속 후 해제 예정).

0-7: 로컬 `.manus-logs/` 삭제, `.gitignore` 에 `.manus-logs/` 추가.

### 6. (정정) ch02 教科書沒說的 절 "수치 0건" 은 사실이 아니었다

위 v14 기록에서 "수치 0건"이라고 적었으나, ch02 유도 질문에 `5~10분` 이 남아 있었다.
해당 수치를 제거하고 프리뷰·artifact 재생성. 재검 결과 ch01 ko·zh / ch02 ko 세 절 모두 0건.
(ch01 의 `18-MEA` 는 화합물 고유명이므로 수치로 세지 않는다.)

### 2-4. (검증) 문서층 vol 표기 — 교체 필요 0건

`drafts/` · `ch*/` · `review-ko/` 전수 검색 결과 vol 은 전부 `6%＝20vol` 형태로
**% 뒤 병기**돼 있어 이미 기준 단위가 % 다. % 없이 vol 만 쓴 줄은 단 1곳
(`18-developer-volume-guide.md:45` — vol 의 정의를 설명하는 문장)이며, 이 글의 주제 자체가
%↔vol 환산이므로 존치가 맞다. 題庫·인용문 내 표기는 손대지 않았다.

### 7. `git rm --cached` 대상 정정

지시서가 지목한 `content-factory/ch01/src/__pycache__/strings.cpython-311.pyc` 는 존재하지 않는다.
실제 추적 중이던 pyc 는 `content-factory/scripts/__pycache__/parse_exam_bank.cpython-314.pyc` 하나이며
이쪽을 인덱스에서 제거했다.

---

## v16 실행 기록 (2026-08-22)

### 0. 접속 차단 — DNS 원인 재확인 (갈래 A 확정 유지)

진단 3종 재실행. v15 와 동일:

| 항목 | 결과 |
|---|---|
| `dig beautyjob.tw A` | `15.197.225.128` / `3.33.251.168` |
| `dig www.beautyjob.tw CNAME` | `cname.manus.space.` |
| `dig beautyjob.tw NS` | `ns52/ns51.domaincontrol.com.` → **GoDaddy 확정** |
| Render 원본 | **200 / 리다이렉트 0** (`x-render-origin-server: Render`) |
| `beautyjob.tw` | 301 → www → 302 → `manus.im/app-auth` (`server: cloudflare`) |

`DNS_CUTOVER.md` 갱신: GoDaddy 는 apex 에 CNAME 을 못 넣는다(ALIAS/ANAME 미지원)는
제약을 명시하고 **apex=A레코드 / www=CNAME** 으로 처리 방법을 분리했다. Render 공식 문서
(<https://render.com/docs/configure-other-dns>) 기준 apex A = `216.24.57.1`, 단 대시보드 값이
다르면 대시보드 우선이라고 못 박았다. 롤백용 원 레코드 보존 블록과 GoDaddy 포워딩 해제
지시도 추가.

### 0-8. ManusDialog / manusTypes 사용처 확인

| 대상 | 사용처 | 조치 |
|---|---|---|
| `client/src/components/ManusDialog.tsx` | **0건** (자기 정의만) | **제거.** "Please login with Manus to continue" + "Login with Manus" 버튼을 렌더하는 컴포넌트였다 — 정확히 다시 뜨면 안 되는 UI |
| `server/_core/types/manusTypes.ts` | **1건** — `server/_core/sdk.ts:16` | **제거하지 않음.** 화면에 렌더되지 않는 타입 전용 파일이고, 이걸 쓰는 sdk 는 현행 이메일·LINE 로그인의 JWT 세션 발급(`routers.ts:701,738`)과 전 요청 인증(`context.ts:17`)을 담당한다. 지우면 인증이 통째로 깨진다 |

### 0-9. vite-plugin-manus-runtime 제거

`package.json` devDependency 삭제 + `pnpm install --lockfile-only` 로 락파일 정리.
`vite.config.ts:152` 에 이미 "플러그인 배열에서 제거 확정(2026-08-20)" 주석이 있어
실사용은 없던 상태였다. **빌드 1회 통과 확인**: `npm run build` → vite ✓ built in 1.85s,
esbuild `dist/index.js 210.5kb`.

### V1~V4

V1 홈 200/0 · V2 `/salon/00147378` 200/0, `/supply-map` 200/0 · V3 robots.txt
User-agent 블록 10개, `Disallow: /` 전면차단 **0건**(단 페이지 meta 는 아직 noindex) ·
V4 잔존 6건 — 전부 사유 있음(dev 전용 디버그 수집기 2, localStorage 잔재 **정리** 코드 1,
휴면 OAuth 분기 1, 문서 1, 위 manusTypes 1).

---

## v18 실행 기록 (2026-08-22) — 도메인 전환 완료 후

### 0/1. 도메인 연결 후속

DNS 전환 완료 확인(게이트): apex `216.24.57.1` → 301 → `www.beautyjob.tw` → 200
(`x-render-origin-server: Render`). Manus 리다이렉트 0.

| 항목 | 조치 |
|---|---|
| 0-1 | `manusTypes.ts` 최상단에 삭제 금지 경고 주석 |
| 0-2 | CLAUDE.md 에 커밋 절차 규칙 추가 (`git diff --cached --stat` 필수 / 앱 코드는 커밋 분리) |
| 0-3 | `DNS_CUTOVER.md` 를 작업 지시서 → **완료 기록**으로 전환. www 우선이 정본 순서 |
| 1-1 | 미푸시였던 `629c6f2` 푸시 |
| 1-2 | noindex 해제. 라이브 실측 확인 — `<meta name="robots" content="index, follow">` |
| 1-3 | 도메인 정본 `https://www.beautyjob.tw` 로 4곳 교체 |
| 1-4 | sitemap 3,422 URL 전부 신도메인 (`DATABASE_URL` 부재로 재생성 불가 → 오리진 치환) |
| 1-6 | 애널리틱스 `%VITE_%` 리터럴 → 런타임 가드. dist 리터럴 2건 → 0건 |

### 1-5. (정정) debug-collector 는 dist 에 **있다**

v16 에서 "프로덕션에 주입 안 됨"이라고 보고했는데, 그 말은 맞지만
**파일 자체는 `dist/public/__manus__/debug-collector.js` 로 배포된다.**
`client/public/` 안에 있어 Vite 가 정적 자산으로 복사한다. index.html 이 참조하지
않으므로 실행되지는 않지만, 공개 도메인에서 URL 로는 접근된다. 제거 여부는 지시 대기.

### 2. 법규 확인 — 6건 확정, **불일치 2건 발견**

근거는 전부 官方 원문: 食藥署 開放資料(化粧品成分使用限制表, 衛授食字第1131602014號)
와 全國法規資料庫(化粧品衛生安全管理法 第20條).

| 순위 | 항목 | 결과 |
|---|---|---|
| ① | 過氧化氫 12% | **확정 — 법정 상한 맞음.** 編號167 「(a)染髮、燙髮產品 12% (40 volumes)(含釋出之H2O2)」 |
| ② | PPD 2% | 일치 (編號65, 以free base計) |
| ② | 阿摩尼亞 6% | 일치 (編號166, 以NH3計) |
| ② | **對甲苯二胺 4%** | ⚠️ **불일치.** 現行은 編號66 「2%(以free base計) / 3.6%(以sulfate salt計)」 |
| ③ | 第20條 罰鍰 | 일치 (4萬～20萬 / 60萬～500萬) |

또 #13 교차검증에서 用途名 1건 차이: 본문 「沖洗之髮用產品」 → 官方은
「**立即**沖洗之髮用產品」(編號33 (b)). 수치·pH 는 일치.
**둘 다 v18 2-3 에 따라 임의 수정하지 않고 지시 대기.**

대장 집계: 현행 9 / 폐지 1 / 미확인 7 (합계 17).

### 3-2. beautyjob.com.tw — Manus 지향 아님

`A 13.248.243.5, 76.223.105.230` / CNAME 없음 / NS `ns63·ns64.domaincontrol.com`.
Manus 계열 호스트(`cname.manus.space`)를 가리키지 않는다. 설정은 건드리지 않았다.

---

## v19 실행 기록 (2026-08-22)

### 1·2. 법규 정정 + 12% 문구

| 대상 | 조치 |
|---|---|
| `#10`·`#26`·**`#30`** 對甲苯二胺 | 4% → 「2% 以下（以 free base 計）／硫酸鹽 3.6% 以下」 병기. **#30 은 v19 지시서 목록 밖이었으나 같은 오류라 함께 정정** |
| `#13` TGA 沖洗 행 | 「沖洗之髮用產品」 → 「**立即**沖洗之髮用產品」 (수치·pH 유지) |
| 계산기 12% | `legalNoteForPercent()` 신설 — 法規 사실만. 시술 권고(`railForPercent`)와 **함수·UI 위치 모두 분리** |

파생물: theory 글에는 파생 HTML 이 없다(artifacts 3종은 ch01·ch02 전용, 실측 0건). 재생성 대상 없음.

### 편번 오기 — 리포 내 0건

지시서 v16~v18 의 「esters 編號167」은 오기(官方 기준 esters=**168**, 167=**過氧化氫**)지만,
**리포 본문·각주·주석·대장 어디에도 잘못 적힌 곳은 없었다.** 대장 #13 행에 編號33·168 을
명시해 다음에 같은 혼동이 없게 못박았다.

### 3. debug-collector — 요청자 있음 → 이동안 적용

dev 서버 실측: `vite.config.ts` 플러그인이 여전히 `<script src="/__manus__/debug-collector.js">`
를 주입하고 파일이 200(25,168 bytes)으로 응답했다. **요청자 있음** 판정.
→ `client/public/__manus__/` → `tools/devtools/` 로 이동, dev 미들웨어가 디스크에서 서빙.
경로·전역키도 `__devtools__` 로 개명. **dist 에 `__manus__` 0건.**

부수 발견: `useAuth.ts` 가 `localStorage["manus-runtime-user-info"]` 에 로그인 사용자 정보를
매 상태 변화마다 **기록**하고 있었다(읽는 쪽 0건). v16·v18 에서 "삭제하는 정리 코드"라고
보고했는데 **틀렸다 — setItem 이었다.** 소비자였던 플러그인이 사라진 뒤 죽은 기록이라 제거했다.

### 4. IndexNow 활성화 + 첫 제출

`INDEXNOW_ENABLED = true`. 전제 확인 — 키 파일 `/{KEY}.txt` 라이브 200, 페이지 `index, follow`.

첫 제출에서 **403** 이 났다. 1건 제출은 200 이라 키는 유효했고, 배치 크기를 10→100→1000→
2000→3000→3422 로 올려도 전부 200 이었다. **최초 403 은 일시적**(API 가 키 파일을 아직
확인하기 전)으로 판단. 정식 재제출 결과:

```
[IndexNow] ENABLED=true KEY=9f2c4b7e8a1d40f3b6c5e2d7a8f14b03
[IndexNow] sitemap URL 수: 3422 / 오리진 불일치 0건
[IndexNow] 결과: {"sent":true,"status":200,"count":3422} (1854ms)
```

### 2번(계속). 법규 미확인 — 배치 ① 8건 확인

| 대상 | 결과 |
|---|---|
| 化粧品衛生安全管理法 2018 전면개정 | ✅ 修正日期 民國107年5月2日 |
| 2019.07.01 시행 | ✅ 衛福部 「108年7月1日正式上路」(標示 규정만 110.07.01) |
| 化粧品 정의 5가지 용도 | ✅ 第3條 潤澤髮膚·刺激嗅覺·改善體味·修飾容貌·清潔身體 |
| PIF/GMP 3단계 일정 | ✅ 衛授食字第1131604608號 — **본문 표 3행이 자구까지 일치** |
| 特定用途化粧品 = 染髮·燙髮 분류 (#23·#25·#26·#30) | ✅ PIF 공고 1단계 대상 명시 |
| #10 產品登錄+PIF | ✅ 第4條第1項 |
| #30 化粧品成分使用限制表 성분 한도 | ✅ 編號65·66·166 |
| #26·#10 間苯二胺 금지 | ⚠️ **금지는 확정**(禁止使用成分表 編號300), 「2009 起」 시점만 미확인 |

**근거 미발견 1건**: `#25:163` 「第20條 罰鍰이 2024/7/1 이후 從嚴適用」 — 罰則은 2019 시행
이후 변동이 없다. 2024-07-01 에 바뀐 건 성분표 개정·PIF 1단계·查驗登記 停止適用이고
**罰則 강화가 아니다.** 수정 지시 대기.

대장 집계: 현행 20 / 조건부 1 / 폐지 1 (합계 22).

### 1. GSC 준비

`DNS_CUTOVER.md` 8절 신설 — 소유권 확인은 **HTML 메타 태그** 권장(SPA 홈이 정적
`index.html` 을 그대로 내려주므로 크롤러가 바로 읽는다). 태그 수령 후 배치 절차와
라이브 확인 방법까지 기재. sitemap 라이브 200 / 3,422 URL / 구도메인 0건 실측.

---

## v21 실행 기록 (2026-08-22) — 라인 B 한국어 마스터 방식 착수

### 공정 변경

v6 의 B5 동시생산(ko·zh 동시)을 폐기하고 **한국어 마스터 방식**으로 바꿨다.
① ko 풀버전 → ② 선후 검수(정지 게이트) → ③ 반영 → 번체 번역 → lint → 커밋.
**검수 전 번체 착수 금지.**

### 1. 題庫 JSON — 재현 확인

`~/Downloads/067003A13美髮丙級學科.pdf` (SHA-256 `368404…a9b1`) 가 리포 사본과 **해시 일치**.
텍스트 추출에서 `女子美髮`(10) `V114112113`(1) `06700`(10) `丙級`(10) 확인.
파서 재실행 결과 JSON 이 기존과 **바이트 동일** — 版次 갱신 시 재실행 가능함이 확인됐다.

G1 섹션 9개·합 608 PASS / G2 결번·중복 0 PASS / G3 answer·보기 무결성 PASS /
G4 스팟체크 6건 PASS. tail 보존 84건, 보기 선행형(stem 없음) 6건,
첨자 재결합 후보 3건(03-027 08-034 08-037).

### 2. 04 문서 정정 — 07 묶음 재설계

v2 의 `exam-perm-01~04` 배정은 B5 방식 산물이라 **폐기**하고 `tangfa-01~04` 로 다시 그었다.
배정 축을 題庫 번호가 아니라 **시술 진행 순서**로 바꿨다. 파티션 검증(중복 0·누락 0) 통과.

| 편 | 주제 | 문항 |
|---|---|---|
| ① | 軟化·試捲·중간 헹굼 | 30 |
| ② | 捲法·分區·底盤·角度 | 28 |
| ③ | 藥劑 화학·pH + 모질 진단 | 26 |
| ④ | 燙後 처리·손상·병행 | 13 |

v2 배정과는 **17題만 겹친다.** 폐기된 배정은 `<details>` 로 기록 보존.
3-2 절 신설 — 공동과목(化粧品·公共衛生 계열) **착수 금지** 명시.

### 3. 제1편 ko 풀버전

`drafts/exam-ko/tangfa-01.md` (11,526자 / 中文 字數 1,019).
문항 원문은 **생성 스크립트가 JSON 에서 직접 인용**한다 — 재타이핑 경로가 없다.
인용 충실도 게이트: md 의 30개 인용줄을 JSON stem+options+tail 과 대조 → **불일치 0건.**

해설 평균 2.7줄 / 「시험 답 vs 현장」 4건 / 이론 슬러그 참조 26건 / 내부링크 26.
lint 통과 (간체 0 · 금지어 0 · B2 30문항 · B3 출처 문구 있음 · R10 슬러그 전부 확정 목록 내).
**법규 수치는 쓰지 않았다** — 이 30문항이 다루는 범위에 대장 등재 수치가 필요한 문항이 없었다.

### 4. iCloud 검수판

`~/Library/Mobile Documents/com~apple~CloudDocs/콘텐츠폴더/beautyjob/`
`燙髮篇01_검수판.pdf` (18쪽) + 동일 내용 HTML.
헤드리스 Chrome(playwright chromium_headless_shell 151) print-to-pdf 사용.

### 정지

**여기서 멈춘다.** 선후 검수 코멘트 수신 전 번체 번역·2편 착수 금지.

---

## v24 — RC 종료 후 통합 (2026-08-22)

### 사고: v22 이중 투입

두 세션이 같은 지시서 v22 §1 을 동시에 작업했다.

| 커밋 | 내용 | 혼입 |
|---|---|---|
| `89aeb2c` | 投保薪資 트리거 표시값 불일치·문구 증식 수정 (v22 1번) | **타 세션 작업 혼입** — 두 세션의 §1 수정이 한 커밋에 섞였다 |
| `5bde4bd` | 계산기 요율·급距 官方 대조 등재 (v22 2번) | **검수 대기분 커밋** — 라인 B `tangfa-01.md` 등 선후 검수 전 산출물이 함께 올라갔다 |

**판정(v24): 둘 다 존치. revert 하지 않는다.**
라인 B 게이트(번체 번역·2편 착수·사이트 게재 금지)는 **그대로 유지**한다 —
`tangfa-01` 커밋본이 원격에 있다는 사실이 게이트 해제를 뜻하지 않는다.

재발 방지 규칙은 `CLAUDE.md` 에 등재했다 (세션명 미기재 블록 실행 금지 /
같은 리포 활성 세션 확인 / 디렉터리 단위 `git add` 금지).

### §1a — 「두 벌 수정」 실측 판정

`89aeb2c` 에는 같은 증상에 대한 수정이 세 층으로 들어갔다. 지우기 전에
어느 층이 실제로 버티고 있는지 로컬 실측(헤드리스 Chrome + CDP)으로 갈랐다.
시나리오: 번역기가 값 노드의 텍스트를 `<font>` 로 옮김 → 급여 32,000→45,000 → 드롭다운 개폐 2회.

| 조합 | translateGuard | 컴포넌트 수정 | 개폐 후 값 노드 | 증식 | 표시값 |
|---|---|---|---|---|---|
| 1 (현행) | v2 | 있음 | 2 | 없음 | 45,800 ✅ |
| 2 | **v1** | 있음 | 2 | 없음 | 45,800 ✅ |
| 3 | v2 | **없음** | 4 | 없음 | 45,800 ✅ |
| 4 (대조군) | **v1** | **없음** | **8** | **증식** | 낡은 사본 잔존 ❌ |

조합 2·3 이 각각 통과하므로 **이 셀렉트 한정으로는 확실히 두 벌**이다.
그러나 세 층이 막는 실패 모드가 서로 다르다 —

| 층 | 막는 것 | 범위 | 다른 층이 대신할 수 있나 |
|---|---|---|---|
| `translateGuard` v2 | 번역기가 노드를 **옮겼을 때** 남는 잔여물 | **앱 전역** | ✕ 컴포넌트 수정은 이 셀렉트만 |
| `SelectValue` children | Radix **포털 복제** 자체를 안 씀 | 이 셀렉트 | ✕ SSR 렌더 테스트 14건이 여기 걸려 있다 |
| `translate="no"` (금액) | 번역기가 금액 노드를 **갈아끼울 때** | 금액 표시 | ✕ 가드는 「옮김」만 처리, 「교체」는 못 막는다 |

**정리 결과: 코드 제거 없음.** 중복으로 보였으나 커버 범위가 갈린다.
한 벌로 줄이면 ①셀렉트 밖 화면의 번역 잔여물 ②렌더 테스트 14건 ③금액 노드 교체
중 하나를 잃는다. 위 표를 근거로 남긴다 — 다음 세션이 「중복이니 지우자」로
되돌아오지 않도록 여기 기록한다.

### §1b — translateGuard 테스트 원본 4건 vs 신규 8건 대조

원본 4건은 `globalThis.Node` 를 모듈 로드 **뒤에** 갈아끼웠고 `installed` 플래그 때문에
가드가 실제로 설치되지 않았다. 그래서 2·3번은 가드가 아니라 **FakeNode 본래 동작**
(예외를 던진다)을 검증하고 있었다. 신규는 `vi.resetModules()` + 동적 import 로
매 테스트마다 가드를 실제로 설치한다 — 그래서 같은 상황의 기대값이 뒤집힌다.

| 원본 4건 | 신규 대응 | 관계 |
|---|---|---|
| 정상 경로에서는 원래대로 자식을 제거한다 | ① 정상 경로: 직계 자식은 그대로 제거된다 | **1:1 승계** |
| 부모가 어긋난 노드를 지우려 하면 예외를 던진다 *(가드 없을 때의 현상)* | ④ 무관한 곳으로 옮겨진 노드는 예외 없이 무시된다 (크래시 회귀) | **기대값 반전** — 가드를 실제로 설치했으므로 던지지 않는 것이 정답 |
| 기준 노드가 남의 자식이면 insertBefore 도 던진다 *(가드 없을 때의 현상)* | ⑦ 기준 노드가 사라졌으면 끝에 붙이고 예외를 던지지 않는다 | **기대값 반전** — 위와 같은 이유 |
| 부모 동일성으로 정상/비정상을 가른다 (판단 조건) | ② 번역기 래퍼 안으로 옮겨진 노드도 실제로 제거된다 | **승격** — 판단 조건 확인에서 실제 동작 검증으로 |
| — | ③ 빈 번역기 래퍼는 함께 정리된다 | 신규 (잔여물) |
| — | ⑤ 이미 떨어져 나간 노드도 예외를 던지지 않는다 | 신규 (크래시) |
| — | ⑥ 기준 노드가 래퍼 안이면 래퍼 자리 앞에 넣어 순서를 지킨다 | 신규 (순서 보존) |
| — | ⑧ 정상 경로: 기준 노드가 직계 자식이면 그 앞에 삽입된다 | 신규 (정상 경로) |

**유실된 커버리지 없음.** 원본 4건의 상황은 모두 신규에 있고, 2·3번은 가드가 실제로
도는 조건에서 기대값만 뒤집혔다. 신규 8건 중 ②③⑥ 은 원본 가드(v1)에서 실패함을 확인했다.
원본 파일은 `git show 35dc4e8:client/src/lib/translateGuard.test.ts` 로 복구 가능하다.

### v25 판정 회신 반영 (2026-08-22)

**판정: translateGuard v2 + SelectValue children + translate=no 3층 존치 — 각기 다른 실패 모드 방어, 실측 매트릭스 근거 (v25)**

**판정: 勞保 40,100 = 官方 표값 1,002 정본. 표에 금액이 있는 급距는 표 조회가 정본, 산식은 교차검증용으로 강등 (v25)**

#### v24 보고 정정 — 40,100 은 「설명 안 되는 특이값」이 아니었다

v24 에서 「銀行家 반올림으로도 설명이 안 된다」고 적었는데 틀렸다.
**官方은 12.5% 를 한 번에 곱하지 않는다. 普通事故 11.5% 와 就保 1% 를 각각 계산해
각각 반올림한 뒤 더한다.**

```
40,100 본인 = round(40,100 × 11.5% × 20%) + round(40,100 × 1% × 20%)
            = round(922.3) + round(80.2) = 922 + 80 = 1,002
단일 12.5%  = round(40,100 × 12.5% × 20%) = round(1,002.5) = 1,003   ← 1원 어긋남
```

이 분리합산 산식은 **11급 × 본인·단위 = 22개 값을 전부 재현한다.**
단일 12.5% 는 본인 1건(40,100) + 단위 3건(29,500·38,200·45,800), 합 4곳이 어긋난다.
즉 어긋남은 특정 급距의 특이값이 아니라 **산식 자체가 官方과 달랐던 것**이다.

조치: `LABOR_PREMIUM_TABLE`(官方 금액 11급 × 본인·단위)을 정본으로 두고,
표에 없는 급距만 분리합산 산식으로 떨어지게 했다. 테스트가 두 경로를 교차검증한다.

### v25 §3 — 세션 자기식별 한계 (기록)

`[실행: mac-test]` 같은 세션 지정을 지시서가 쓰는데, **세션이 자기 이름을 확인할 방법이 없다.**
`ListAgents` 는 다른 세션(peer)만 보여주고 자기 자신은 목록에 없다.
그래서 「내가 mac-test 인가」를 검증할 수 없고, "다른 세션 실행 금지"는
**다른 세션이 스스로 자제하는 것**으로만 지켜진다.

당면 운용: 같은 리포 앱 트랙은 mac-test 단독 원칙. 세션 시작 시 활성 세션 수를 세어 보고한다
(그건 가능하다). 자기식별이 필요해지면 지시서가 세션마다 다른 토큰(예: 파일 경로)을
주는 방식이 대안이다 — 지금은 한계만 기록해 둔다.

### v26 판정 회신 반영 (2026-08-22)

**판정: openingCost 勞退 급距 조회 교체, 32,000 기준 6,445→6,523 — 산식 출력값이지 실측 검증값 아님 (v26)**

`min(실급여, 150,000) × 6%` → `月提繳分級表 급距 × 6%`.
급여 계산기(v24 自提)와 **같은 표**(`insuranceTables2026` 勞退 62급)를 쓴다 — 산식 이중화 없음.

연쇄로 바뀐 값 (전부 종전 산식의 출력값이지 실측 검증값이 아니다):

| 항목 | 종전 | v26 |
|---|---|---|
| 勞退 고용주분 (월급 32,000) | 1,920 | **1,998** |
| 고용주 부담 합계 | 6,445 | **6,523** |
| 인건비 실부담 | 38,445 | **38,523** |
| 월 고정비 (개점 카드②) | 96,445 | **96,523** |
| 월 고정비 (BEP 앵커) | 88,445 | **88,523** |
| BEP | 243,125 | **243,340** |
| 권장 준비자금 (6개월) | 1,656,670 | **1,657,138** |

### v26 감사 — 37개 에이전트 병렬 검증 결과 (2026-08-22)

v26 변경을 5축(낡은 숫자·코드 정합성·官方 대조·동종 결함·문서 정합성)으로 병렬 검토하고
각 발견을 반증 지향으로 재검증했다. 발견 27건 중 확정 9건 / 기각 18건.

#### 🔴 감사가 잡은 것 — 官方 표 오염

검증 에이전트 하나가 「이 배열을 아무도 안 읽는다」를 실증하려고
`LABOR_INSURANCE_TABLE.amounts` 의 1·2급을 **29,500·30,300 → 31,000·32,000** 으로
바꿔놓고 되돌리지 않았다. 官方에 없는 값이고 오름차순도 깨졌는데
**315개 테스트가 전부 통과했다.** 아무도 그 배열을 읽지 않았기 때문이다.

복원하고, 그 구멍을 막았다:

| 조치 | 내용 |
|---|---|
| 사본 제거 | `rates2026.INSURED_SALARY_BRACKETS` 를 손복사본에서 **`LABOR_INSURANCE_TABLE.amounts` 파생**으로 교체 |
| 오염 감지 | 官方 11급 값을 그대로 단언하는 테스트 + 네 표 오름차순 검사에 勞保 표 편입 |
| 실증 | 같은 오염을 다시 주입하자 **8건 실패**(종전 0건). 복원 후 317건 통과 |

**교훈: 쓰기 권한을 가진 검증 에이전트는 작업트리를 오염시킬 수 있다.**
병렬 감사 뒤에는 반드시 `git diff` 로 의도하지 않은 변경을 확인한다.

#### 확정 발견 및 조치

| 발견 | 조치 |
|---|---|
| `BizSimulator.tsx` 기본 월 고정비가 96,445 로 굳어 있음 | 리터럴 제거 → `calcStaffCost` 로 **파생**. 요율이 바뀌면 자동 추종 |
| `rates2026.ts` `PENSION_EMPLOYER_RATE` 주석이 「실급여 기준」 | 「月提繳分級表 급距 기준」으로 교정 (v26 변경과 정면 모순이었다) |
| 勞保 분급표 이중 정의 | 위 「사본 제거」 |
| 대장 집계표가 실제 행 수와 2건 어긋남 | 20행 기준으로 정정. 조건부 1건은 v24 해소분이라 현행으로 이동 |
| v25 「판정 대기」 절을 덮어씀 | `<details>` 로 복원 — v24 오판 기록과 같은 방식 |
| 고용주 부담 라벨 `勞退提繳（6%）` | `（月提繳工資 6%）` 로 교정. 6% 가 곱해지는 건 화면 위 月薪 이 아니다 |

#### 범위 밖으로 남긴 것 (판정 대기)

- 급여란을 비운 채 직원 수 1 이면 고용주 부담 4,100 이 붙는다(勞保 2,582 + 健保 1,428 + 勞退 90).
  세 급距가 모두 제1급으로 떨어져서다. v25 표 분리 때 생긴 기존 동작이고 v26 이 만든 건 아니다.
- `PENSION_SELF_RATE_MAX`·`PENSION_EMPLOYER_MAX_BASE`·`LABOR_INSURANCE_RATE` 는 참조 0건.
  이 파일의 「문서용 상수」 관행이라 기각했다.
- `toolStrings.ratesNote` 의 요율 숫자(29,500·12.5%·5.17%)는 여전히 리터럴이다.
  연도만 메타에서 파생된다.
